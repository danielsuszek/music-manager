{% extends '::base.html.twig' %}

{% block body -%}

                <h1 class="list-album-header">LISTA ALBUMÓW</h1>
                <div class="row">
                  <div class="col-sm-12">
                    <div class="text-center">
                      <a href="{{ path('album_new') }}"><button class="btn btn-primary" id="singlebutton">Dodaj album</button></a>
                          
                    </div>
                  </div>
                </div>
                <div class="list-group">
                    {% for entity in entities %}
                        <a href="{{ path('album_show', {'id': entity.id}) }}" class="list-group-item"> 
                            <img src="{{ entity.getFullPathToSleeve() }}" alt="" class="picture-small" />                             
                            <span class='band-name'>{{ entity.name }} ({{ entity.released }}) - {{ entity.band.name }}</span>
                        </a>
                    {% endfor %}
                </div>
{#                <table class="records_list">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Name</th>
                            <th>Released</th>
                            <th>Description</th>
                            <th>Rate</th>
                            <th>Sleevepicurl</th>
                            <th>Bandid</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for entity in entities %}
                        <tr>
                            <td><a href="{{ path('album_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                            <td>{{ entity.name }}</td>
                            <td>{{ entity.released }}</td>
                            <td>{{ entity.description }}</td>
                            <td>{{ entity.rate }}</td>
                            <td>{{ entity.sleevePicUrl }}</td>
                            <td>{{ entity.bandId }}</td>
                            <td>
                            <ul>
                                <li>
                                    <a href="{{ path('album_show', { 'id': entity.id }) }}">show</a>
                                </li>
                                <li>
                                    <a href="{{ path('album_edit', { 'id': entity.id }) }}">edit</a>
                                </li>
                            </ul>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>

                    <ul>
                    <li>
                        <a href="{{ path('album_new') }}">
                            Create a new entry
                        </a>
                    </li>
                </ul>
#}
{% endblock %}
